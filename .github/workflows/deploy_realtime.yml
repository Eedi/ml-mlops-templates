on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      environment_config_file:
        required: true
        type: string
      run_load_test:
        required: true
        type: boolean
      load_test_config:
        required: false
        type: string
        default: ''
      load_test_name:
        required: false
        type: string
      endpoint_name:
        required: true
        type: string
      endpoint_config:
        required: true
        type: string
      deploy_blue:
        required: true
        type: boolean
        default: false
      deploy_green:
        required: true
        type: boolean
        default: false
      blue_deployment_name:
        required: false
        type: string
      green_deployment_name:
        required: false
        type: string
      blue_deployment_config:
        required: false
        type: string
      green_deployment_config:
        required: false
        type: string
      blue_traffic_percentage:
        required: false
        type: number
      green_traffic_percentage:
        required: false
        type: number
      blue_mirror_percentage:
        required: false
        type: number
      green_mirror_percentage:
        required: false
        type: number

jobs:
  get-config:
      uses: Azure/mlops-templates/.github/workflows/read-yaml.yml@main
      with:
        file_name: ${{ inputs.environment_config_file }}

  deploy:
    needs: get-config
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      id-token: write
      contents: read
    env:
      resource_group: ${{ secrets.PROJECT_RESOURCE_GROUP_NAME }}
      location: ${{ secrets.PROJECT_RESOURCE_GROUP_LOCATION }}
      aml_workspace: mlw-${{ needs.get-config.outputs.namespace }}-${{ needs.get-config.outputs.postfix }}${{ needs.get-config.outputs.environment }}
      keyvault_name: kv-${{ needs.get-config.outputs.namespace }}-${{ needs.get-config.outputs.postfix }}${{ needs.get-config.outputs.environment }}
      app_insights_name: appi-${{ needs.get-config.outputs.namespace }}-${{ needs.get-config.outputs.postfix }}${{ needs.get-config.outputs.environment }}
      load_test_resource: lt${{ needs.get-config.outputs.namespace }}${{ needs.get-config.outputs.postfix }}${{ needs.get-config.outputs.environment }}
      endpoint_name: ${{ inputs.endpoint_name }}
      endpoint_config: ${{ inputs.endpoint_config }}
      blue_deployment_name: ${{ inputs.blue_deployment_name }}
      green_deployment_name: ${{ inputs.green_deployment_name }}
      blue_traffic_percentage: ${{ inputs.blue_traffic_percentage }}
      green_traffic_percentage: ${{ inputs.green_traffic_percentage }}
      blue_mirror_percentage: ${{ inputs.blue_mirror_percentage }}
      green_mirror_percentage: ${{ inputs.green_mirror_percentage }}
      load_test_config: ${{ inputs.load_test_config }}
      load_test_name: ${{ inputs.load_test_name }}
      envname: ${{ inputs.environment }}
      action_group_name: ag-${{ needs.get-config.outputs.namespace }}-${{ needs.get-config.outputs.postfix }}-${{ needs.get-config.outputs.environment }}
    steps:
      - name: Set variables
      - id: variables
        run: |
          storage_account="st${{ needs.get-config.outputs.namespace }}${{ needs.get-config.outputs.postfix }}${{ needs.get-config.outputs.environment }}"
          storage_account="${storage_account//-/}" # remove dashes
          echo "storage_account=$storage_account" >> $GITHUB_OUTPUT
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Debug ref
        run: |
          echo "Checked out ref: $(git rev-parse --abbrev-ref HEAD)"
          git log -1
      - name: Check out scripts
        uses: actions/checkout@v4
        with:
          repository: Eedi/ml-mlops-templates
          token: ${{ secrets.GITHUB_TOKEN }}
          path: ml-mlops-templates
      - name: Check blue-green deployment logic
        run: |
          ml-mlops-templates/.github/scripts/blue_green_logic_checks.sh
      - name: Az CLI Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.ARM_CLIENT_ID }}
          tenant-id: ${{ secrets.ARM_TENANT_ID }}
          subscription-id: ${{ secrets.ARM_SUBSCRIPTION_ID }}
      - name: Create endpoint
        if: inputs.deploy_blue || inputs.deploy_green
        run: ml-mlops-templates/.github/scripts/create_endpoint.sh
      - name: Create or update endpoint diagnostic settings
        run: ml-mlops-templates/.github/scripts/create_or_update_diagnostic_settings.sh
      - name: Blue deployment
        if: inputs.deploy_blue
        run: | 
          echo "Doing blue deployment"
          ml-mlops-templates/.github/scripts/deploy.sh \
            -e ${{ inputs.endpoint_name }} \
            -n ${{ inputs.blue_deployment_name }} \
            -c ${{ inputs.blue_deployment_config }} \
            -w ${{ env.aml_workspace }} \
            -r ${{ env.resource_group }} \
            -t ${{ inputs.blue_traffic_percentage }} \
            -s ${{ steps.variables.outputs.storage_account }}
      - name: Green deployment
        if: inputs.deploy_green
        run: |
          echo "Doing green deployment"
          ml-mlops-templates/.github/scripts/deploy.sh \
            -e ${{ inputs.endpoint_name }} \
            -n ${{ inputs.green_deployment_name }} \
            -c ${{ inputs.green_deployment_config }} \
            -w ${{ env.aml_workspace }} \
            -r ${{ env.resource_group }} \
            -t ${{ inputs.green_traffic_percentage }} \
            -s ${{ steps.variables.outputs.storage_account }}
      - name: Route traffic
        run: ml-mlops-templates/.github/scripts/route_traffic.sh
      - name: Setup alerts
        run: ml-mlops-templates/.github/scripts/setup-alerts.sh
      - name: Run load test
        if: inputs.run_load_test
        run: ml-mlops-templates/.github/scripts/create_load_test.sh
